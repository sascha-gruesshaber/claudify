# Gates — what must pass, and what "green" means here

**Run these in this order.** The order is written down because somebody already lost an hour to the
other one.

```bash
<format command>              # <what it fixes>
<lint command>                # <where it must run>
<build command>               # <the strict form CI uses>
<test command>                # <what it includes: coverage? which projects?>
<generated-file command>      # integration only — never in a per-ticket worktree
```

## Where a local run is weaker than CI

**This is the most valuable section in this file.** A local command that is not the CI command is not
the gate.

| Locally | In CI | So |
|---|---|---|
| `<command>` | `<the same command plus the strict flag>` | run the CI form before you claim it passes |
| <warnings are shown> | <warnings are errors> | <the flag that reproduces it> |

CI is defined in [`<path to the CI file>`](../<path to the CI file>). **That file is the authority on
what blocks a merge**, not this one — if they disagree, fix this one.

## Generated files

Never regenerate these inside a per-ticket worktree; every parallel tree would rewrite the same file
and conflict for no reason. They are built **once, at integration**.

- `<path>` — regenerated by `<command>`

## Coverage and scanners

- **Floor:** <n> % on new code, measured by <tool>, enforced by <where>.
- **Authority when two tools disagree:** <tool>. <One line on the known disagreement.>
- **You never reach the floor by widening an exclusion or by writing a test that passes by
  construction.** Fix the seam.

## When a gate fails in a way that makes no sense

Read [`<path to a troubleshooting document>`](../<path>) before debugging your own code.

Known traps in this repo:

- <the formatter and a specific analyzer rule deadlock on one construct>
- <a tool that silently does nothing when given the wrong argument shape>
